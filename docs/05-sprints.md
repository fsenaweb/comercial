# Sprints (Backlog Técnico)

Backlog sequencial. Cada sprint mapeia para uma fase do `04-roadmap.md`. Convenção de nomenclatura e padrões em `02-design-patterns.md`; modelo de dados em `03-database-modeling.md`; comandos de validação em `07-dev-environment.md`.

Toda sprint segue o fluxo de trabalho do `CLAUDE.md` (branch própria → desenvolvimento → validação com tudo verde → aprovação → commit único) e inclui testes automatizados como parte da entrega (cobertura ampla — não é item à parte, é critério de "pronto").

## Sprint 0: Fundação (Fase 0) — ✅ CONCLUÍDA (2026-07-11, branch `feat/sprint-0-fundacao`)
- [x] Inicializar projeto Laravel (API) e Nuxt (SPA, `ssr: false`) — *entregue com Nuxt 4 (estrutura `app/`) e, logo em seguida, migrado de Laravel 11 para 12 (11 já fora da janela de suporte de segurança).*
- [x] Configurar `docker-compose.yml`: `postgres` (+ init do banco de testes), `php-fpm`, `nginx` (serve SPA + fastcgi `/api`, `/sales`, `/sanctum`), `nuxt-build` (profile `build`, gera a SPA no volume `nuxt_dist`), `scheduler` (cron). Todos com `restart: unless-stopped`; imagem PHP com UID/GID do host (sem arquivos root no repo).
- [x] Configurar conexão do banco de dados no Laravel (pgsql, `pt_BR`, `America/Sao_Paulo`).
- [x] Migration de `users` já com `role` (enum: admin/cashier/seller), `commission_percent`, `active`, soft deletes — sem tabelas `roles`/`permissions`.
- [x] Autenticação via Laravel Sanctum **modo SPA (cookie de sessão)**: `LoginAction`, login/logout/me, exceptions de domínio (`InvalidCredentialsException`, `InactiveUserException`), middleware `role:`, `StoreSettingPolicy`, API-only sem redirects.
- [x] Tela de Login no Nuxt, `useAuthStore` (Pinia), `useApi`/`useApiError`, middleware global de rota, layout base com logout.
- [x] Migration + endpoints de `store_settings` (registro único via `StoreSetting::current()`) e tela de configuração da loja.
- [x] Configurar `spatie/laravel-backup`: dump local diário via `scheduler` (clean 01:30, run 02:00), disco dedicado `backups`.
- [x] Seeder do admin inicial (`admin@loja.local` / `password` — trocar em produção).
- [x] Testes: 15 feature tests passando — login/logout/perfil (incl. usuário inativo e validações), autorização por papel em `store_settings`, e `BackupRestoreTest` que **restaura** o dump em banco descartável e confere os dados.
- [x] Validação end-to-end via nginx (porta 80): CSRF → login → sessão → logout, typecheck e build da SPA limpos.
- [x] `docs/08-design-system.md` criado (paleta marca amarelo/preto + status semântico, tipografia Bricolage Grotesque/Hanken Grotesk, padrões de componente), extraído de telas e CSS de referência do AppLoja + logo da loja.
- [x] Telas da Sprint 0 retrocedidas para os tokens do design system (`main.css` com `@theme`, `BaseButton`/`BaseInput`/`login.vue`/`index.vue`/`settings/store.vue`/`layouts/default.vue`); tema fixado sempre claro (`color-scheme: light`, sem seguir o SO) e logo da loja no card de login.
- [x] Bug de componente corrigido: `components: [{ pathPrefix: false }]` no `nuxt.config.ts` — sem isso, `<BaseButton>`/`<BaseInput>` não resolviam em runtime (build/typecheck passavam normalmente; só aparecia com o app rodando no navegador). Documentado em `07-dev-environment.md`.
- [x] Hostname local `loja.local` via `/etc/hosts` + `SANCTUM_STATEFUL_DOMAINS`/`SESSION_DOMAIN=null`, ensaiando a exigência de produção (acesso por IP/hostname da LAN, não `localhost`).

## Sprint 1: Cadastros (Fase 1) — ✅ CONCLUÍDA (2026-07-11, branch `feat/sprint-1-cadastros`)
- [x] API CRUD (Model, Migration, Controller, Form Requests, Resources, Policy) para `categories`, `subcategories`, `brands`, `units`.
- [x] API CRUD para `products` + `product_variations` (SKU), incluindo registro do estoque inicial via `stock_movements` (nunca gravar `current_quantity` direto) — a migration de `stock_movements` entra aqui por dependência. *`CreateProductVariationAction` cria a variação e o `stock_movements` (tipo `adjustment`, origem "estoque inicial") dentro de `DB::transaction()`; updates de variação nunca tocam `current_quantity`.*
- [x] API CRUD para `customers` e `suppliers`. *`customers` com policy diferenciada: admin/cashier/seller podem criar e editar, só admin exclui — cliente é cadastrado no fluxo de venda por qualquer papel.*
- [x] Telas Nuxt de listagem e formulário para todas as entidades acima (componentes de tabela/formulário reutilizáveis em `components/ui/`: `BaseSelect`, `BaseTextarea`, `BaseTable` genérica, além de `BaseInput`/`BaseButton` já existentes).
- [x] Máscaras no frontend: moeda, CPF/CNPJ, CEP (composables testáveis: `useCurrencyMask`, `useDocumentMask`, `useCepMask`).
- [x] Testes: feature tests de CRUD (create/update/delete/list + validação de Form Request + autorização por papel) para cada entidade acima (70 testes no total, incluindo o backend pré-existente); Vitest para os composables de máscara (11 testes) — `vitest` adicionado como devDependency (`npm run test`).
- [x] Validação end-to-end via nginx: `docker compose exec php-fpm php artisan test` (70 passed), `npx nuxi typecheck` e `npm run generate` limpos, telas verificadas no navegador (Playwright headless) incluindo criação de categoria, produto e variação com estoque inicial.
- [x] Armadilha nova documentada: o serviço `nuxt-build` do compose faz `COPY frontend/` **na imagem** (não é bind mount) — editar código do front e rodar só `docker compose --profile build run --rm nuxt-build` serve a build antiga. É preciso `docker compose build nuxt-build` antes (ver `07-dev-environment.md`).

## Sprint 2: Caixa (parte da Fase 2) — ✅ CONCLUÍDA (2026-07-15, branch `feat/sprint-2-caixa`)
- [x] Migrations e Models para `cash_registers`, `cash_operations`, `payment_methods` (+ seeder de formas de pagamento padrão).
- [x] `OpenCashRegisterAction` e `CloseCashRegisterAction`, com regra de **um caixa aberto por vez** (`CashRegisterAlreadyOpenException`) e lançamentos de sangria/reforço. *Ampliado com `UpdateCashRegisterAction` (edição de abertura/observação com o caixa ainda aberto) e `RemoveCashOperationAction` (remove um lançamento indevido antes do fechamento — depois de fechado o extrato é imutável), decisões motivadas pelo layout de referência (`Caixa.dc.html`) recebido do usuário.*
- [x] Endpoints de abertura/fechamento/consulta de operações do caixa atual — *mais `GET /cash-registers` (histórico completo, com filtro por status/busca), `PUT /cash-registers/{id}` e `DELETE /cash-registers/operations/{id}`, necessários pra tela de referência.*
- [x] Tela de abertura/fechamento de caixa no Nuxt (`useCashRegisterStore`) — `pages/cash-register.vue`, réplica funcional do layout `Caixa.dc.html` (lista com KPIs/filtro/busca → abertura → detalhe com edição/operações/fechamento) usando os componentes reais do design system (`StatCard`, `StatusBadge`, `BaseModal`-free inline flow). Ações restritas a admin/cashier (`auth.user.role`); link ativado em `AppSidebar.vue`. Validado ponta a ponta no navegador (Playwright): abrir → sangria → reforço → saldo/KPIs corretos → fechar → histórico.
- [x] Testes: feature tests para abrir/fechar caixa (incluindo tentativa de abrir um segundo caixa, fechar caixa já fechado, sangria/reforço) — 27 testes novos em `tests/Feature/CashRegister/` (abertura, fechamento, edição, operações, listagem, formas de pagamento).
- [x] *Extra pós-sprint:* CRUD completo de `payment_methods` (`store`/`show`/`update`/`destroy`, `PaymentMethodPolicy`, hard delete — sem soft delete, conforme `03-database-modeling.md`) — decisão do usuário de não deixar a lista de formas de pagamento hardcoded, já que cadastro é o mesmo custo de manter um enum fixo e é mais flexível pra loja ajustar sozinha. Tela em `settings/catalog.vue` via `CatalogEntityCard` (novo tipo de campo `switch`, reutilizável por outros cadastros futuros). 9 testes novos em `tests/Feature/PaymentMethod/PaymentMethodTest.php` (substituindo os 2 testes de listagem que estavam em `tests/Feature/CashRegister/`).

## Sprint 3: PDV, Venda e Comprovante (parte da Fase 2 — o coração do sistema) — ✅ CONCLUÍDA (2026-07-12, branch `feat/sprint-3-pdv-venda`)
- [x] Migrations para `sales` e `sale_items` (referenciando `seller_id`, `cash_register_id`). *Ampliado com `discount_type`/`discount_value`/`discount` em ambas as tabelas — desconto aceita valor fixo (R$) ou percentual, decisão do usuário; o valor absoluto resolvido fica sempre persistido, nunca recalculado depois. Novo enum `DiscountType`.*
- [x] Interface do PDV no Nuxt (layout dedicado `pos.vue`, foco em teclado): leitura de código de barras (índice `Map` por `ean_gtin`/`product_code` para lookup O(1), campo de busca sempre focado, Enter inclui automaticamente ou abre card de confirmação), busca por nome, carrinho (`useCartStore`), seleção de vendedor (pré-selecionado pelo login, trocável conforme `store_settings.require_seller_on_sale`) e cliente (opcional, com modal "Trocar cliente" e cadastro rápido "Novo cliente"), desconto por item e no total (`DiscountInput`, fixo ou percentual). *Layout adaptado de referência visual recebida do usuário (`PDV.dc.html`, Claude Design) — sem NF-e/NFC-e (fora de escopo), sem modal de abertura de caixa duplicado (reaproveita `/cash-register`), sem seletor de formato de cupom (só térmico 80mm), cores ajustadas para a marca (amarelo/preto) em vez do verde do mockup. Pagamento único por venda nesta entrega — split payment/parcelas e modal "Ajustes" ficam para uma iteração futura.*
- [x] `RegisterSaleAction` dentro de `DB::transaction()` com `lockForUpdate()` no caixa aberto e nas `product_variations` envolvidas (sempre em ordem crescente de `id`, evita deadlock entre terminais): valida caixa aberto (`CashRegisterClosedException`), cria `sale` + `sale_items`, gera `stock_movements` (saída, permite negativo) e `cash_operations` (entrada, `origin: sale` — fecha a lacuna deixada em aberto desde a Sprint 2), decrementa `current_quantity`, gera número sequencial (`V000001`). *Endpoint novo `GET /users/active` (qualquer papel autenticado, só id/nome) — necessário porque o seletor de vendedor do PDV não podia depender de `GET /users`, admin-only.*
- [x] Rota Blade do comprovante (`/sales/{id}/receipt`), CSS `@page { size: 80mm auto }`, marcação "DOCUMENTO NÃO FISCAL", código de barras Code128 (`picqer/php-barcode-generator`) do nº da venda. Front abre em nova janela e chama `window.print()`. *Exigiu corrigir uma lacuna real em `bootstrap/app.php`: `statefulApi()` só liga o middleware de sessão da SPA ao grupo `api`, não a `routes/web.php` — sem o middleware explícito na rota, o cookie nunca seria reconhecido; e `redirectGuestsTo(fn () => null)` caía num fallback `route('login')` inexistente (500) assim que a rota deixou de forçar JSON — corrigido para redirecionar ao `/login` da SPA.*
- [x] Testes: feature tests do fluxo completo de venda (caminho feliz por papel, sem caixa aberto, estoque indo negativo, validações, desconto fixo e percentual, vendedor obrigatório, forma de pagamento restrita a `active_on_pos`) em `RegisterSaleTest` (20 testes); `SaleConcurrencyTest` — primeiro teste de concorrência do projeto, dois processos reais via `Process::start()` contra `comercial_testing` (`DatabaseMigrations`, não `RefreshDatabase`) usando um comando artisan de apoio (`test:concurrent-sale`), confirmando que `lockForUpdate()` evita "lost update" no estoque; `ReceiptTest` (renderização autenticada e redirect de guest). Vitest para `cartMath` (espelha o arredondamento do backend, bcmath) e `usePercentageMask`. Validado also ponta a ponta no navegador (Playwright): login → PDV → busca por código exato e por nome → desconto percentual → forma de pagamento → finalizar venda → comprovante abre com o barcode e os valores corretos → estoque e caixa conferidos no banco.
- [x] *Extra pós-sprint (refinamento a pedido do PM):* campo `products.active` (boolean, default `true`, mesmo padrão de `payment_methods.active_on_pos`/`users.active`) — produto descontinuado some da busca do PDV sem apagar histórico; toggle "Produto ativo" na tela de cadastro, badge "Inativo" na listagem. Distinto do problema de **duplicidade de cadastro** relatado pelo PM: para esse, em vez de inativar, a solução foi um aviso não-bloqueante de "produto parecido já cadastrado" (comparação client-side por substring, sem endpoint novo) tanto no modal completo quanto no Cadastro Rápido — ataca a causa (recadastro por não achar o produto já existente), não só o sintoma.
- [x] *Extra pós-sprint:* `cash_operations.reference_id` (mesmo padrão de `stock_movements.reference_id`, preenchido pelo `RegisterSaleAction` com o id da venda) + botão "Ver itens da venda" na tabela "Operações do caixa" (`cash-register.vue`), abrindo um modal com produtos/quantidades/descontos/forma de pagamento da venda (reaproveita `GET /sales/{id}`, sem endpoint novo). *Bug real encontrado e corrigido no processo: a rota do comprovante (`routes/web.php`) tinha `EnsureFrontendRequestsAreStateful` explícito, duplicando o `StartSession` (a rota já roda no grupo `web` automático do Laravel, que já inicia sessão) — isso deslogava o usuário ao abrir o comprovante. `auth:sanctum` sozinho já basta, o guard resolve a sessão via guard `web` sem precisar daquele middleware aqui.* Migrations de `products.active` e `cash_operations.reference_id` consolidadas diretamente nas migrations de criação das tabelas (não como `add_*` avulsas) — decisão do usuário, já que nada estava em produção ainda.

## Sprint 4: Estoque avançado & Histórico (Fase 3) — ✅ CONCLUÍDA (2026-07-13, branch `feat/sprint-4-estoque-historico`)
- [x] `AdjustStockAction` (ajuste manual) e `RegisterStockEntryAction` (entrada de mercadoria) — únicas vias de escrita em `product_variations.current_quantity`, sempre via `stock_movements`, ambas em `DB::transaction()` + `lockForUpdate()`. `StockMovementController` (`GET /stock-movements` Kardex com filtros de variação/tipo/busca/período, `POST /stock-movements/adjustment`, `POST /stock-movements/entries`) e `StockMovementPolicy` (escrita restrita a admin/cashier, leitura livre a qualquer papel autenticado).
- [x] Tela de ajuste manual de estoque no Nuxt (`pages/stock/adjustments.vue`) — busca produto já cadastrado, informa a quantidade contada (não a diferença) e o motivo; a diferença é calculada e exibida antes de salvar.
- [x] Tela de Kardex (`pages/stock/kardex.vue`): histórico de todas as movimentações com filtros por produto/tipo/período, paginação via "Carregar mais".
- [x] Telas/endpoints de histórico de vendas (`pages/sales-history.vue`, filtros por vendedor/período — `seller_id`/`date_from`/`date_to` em `GET /sales`) e histórico de caixas (filtro de período adicionado à listagem já existente em `cash-register.vue`/`GET /cash-registers`, client-side já que a tela carrega todos os registros de uma vez).
- [x] Testes: feature tests para `AdjustStockAction`/`RegisterStockEntryAction` (autorização por papel, validação), `StockMovementConcurrencyTest` (dois processos reais disputando a mesma variação, mesmo padrão do `SaleConcurrencyTest` da Sprint 3, via novo comando de apoio `test:concurrent-stock-entry`), listagem do Kardex com filtros, e filtros de histórico de vendas/caixa — 175 testes no total (32 novos), tudo verde.
- [x] *Extra pós-sprint (a pedido do usuário, referência visual `EntradaEstoque.dc.html` do projeto AppLoja no Claude Design):* tela dedicada "Entradas de Estoque" (`pages/stock/entries.vue`), separada do ajuste manual — lista entradas (Kardex filtrado por tipo `in`) e formulário de nova entrada com busca de produto já cadastrado, quantidade e motivo/origem em texto livre. Escopo reduzido em relação ao mockup original: importação de XML de nota fiscal, dados estruturados de fornecedor/NF/frete e "gerar contas a pagar" ficaram de fora por decisão do usuário — já reservados para o Sprint 6 (`stock_entries`, `accounts_payable`). A tela "Etiquetas" do mesmo mockup (impressão de código de barras/preço) virou placeholder "Em breve" no sidebar e item novo no backlog (ver "Melhorias transversais").
- [x] Sidebar (`AppSidebar.vue`): grupo "Estoque e Compras" deixou de ser placeholder — ganhou "Entradas de Estoque", "Ajuste de Estoque", "Kardex" (funcionais) e "Etiquetas" (em breve); "Vendas" adicionado ao "Acesso rápido", apontando para `/sales-history` (não `/sales` — colide com o prefixo reservado pelo nginx para a rota Blade do comprovante térmico, `^/(api|sales|sanctum)`; bug real encontrado e corrigido durante a validação end-to-end desta sprint).

## Sprint 4.1: Correções pós-revisão do PM (Fase 3) — ✅ CONCLUÍDA (2026-07-13, branch `feat/sprint-4.1-cancelamento-estoque-atacado-operador`)
Revisão do PM após a Sprint 4 apontou 5 pontos; 4 foram endereçados nesta sprint (o quinto, cotação/orçamento, ficou só registrado — ver Sprint 4.2 abaixo).
- [x] **Cancelamento de venda, sem apagar nada.** `CancelSaleAction` (novo, espelha `RegisterSaleAction`): dentro de `DB::transaction()` com `lockForUpdate()` na venda e nas `product_variations` envolvidas, devolve os itens ao estoque (`StockMovement` tipo `in`, origem "cancelamento venda {number}"), lança uma `CashOperation` compensatória (tipo `out`, origem `adjustment`, **sem apagar o lançamento original da venda**) no caixa aberto atual, e marca `sales.status = canceled` + novos campos `canceled_reason`/`canceled_at`/`canceled_by` — o enum `SaleStatus::Canceled` já existia desde a Sprint 3 mas nunca tinha sido usado. `POST /sales/{sale}/cancel` (`CancelSaleRequest`, motivo obrigatório), `SalePolicy::cancel` restrito a admin/cashier (mesmo padrão de `StockMovementPolicy`). Filtro `status` adicionado a `GET /sales`. **Ressalva do PM atendida:** tanto a operação original da venda quanto o estorno continuam visíveis na listagem de operações do caixa (`cash-register.vue`) — a linha da venda cancelada ganha o badge "Venda cancelada" (novo relacionamento `CashOperation::sale()`, via `reference_id`, exposto em `CashOperationResource` como `sale_status`/`sale_number`); nada é filtrado ou ocultado por padrão. Tela `sales-history.vue` ganhou badge de status, filtro (Todas/Concluídas/Canceladas), botão "Cancelar" (admin/cashier, com modal de motivo) e excluiu vendas canceladas do "Total do período"/"Ticket médio" sem removê-las da lista. *Achado durante a implementação:* o botão "Remover" de operações de caixa (existente desde a Sprint 2) permitia excluir **qualquer** lançamento, inclusive os gerados por venda — o que abriria uma brecha pro mesmo problema que o PM queria fechar. `RemoveCashOperationAction` passou a rejeitar remoção de lançamentos com origem `sale`/`adjustment` (só sangria/reforço manuais podem ser removidos), e o botão some da tela pra essas origens.
- [x] **Estoque nunca fica negativo.** `RegisterSaleAction` agora valida, item a item, que `current_quantity >= quantity` antes de gravar a venda (aborta a transação inteira com 422 se faltar estoque para qualquer item — nenhuma venda parcial); `AdjustStockRequest.new_quantity` ganhou `min:0`. Reverte a decisão registrada na Sprint 3 ("estoque indo negativo" era o comportamento antigo, intencional até então).
- [x] **Margem de lucro automática.** O campo `markup` (decimal, já existia em `product_variations` desde a Sprint 1 mas só aparecia solto no modal de edição) passou a calcular o preço de venda ao vivo (`preço = custo × (1 + margem/100)`) nos 3 formulários de produto (cadastro rápido, SKU, edição) — preço de venda continua editável manualmente depois do cálculo (o recálculo só dispara ao editar custo ou margem, nunca ao editar o preço direto).
- [x] **Preço de atacado por quantidade** (pedido novo do usuário durante a sprint, referência visual do AppLoja — campos "Qtd. p/ Preço Atacado"/"Preço Atacado"). Novos campos `product_variations.wholesale_min_qty`/`wholesale_price` (nullable, opcional). `RegisterSaleAction` grava `sale_items.is_wholesale` (novo campo, pro comprovante/relatório saberem qual preço foi usado). *Revisado durante a validação com o usuário:* a primeira versão aplicava o preço de atacado automaticamente por quantidade, só com um badge informativo no carrinho — só que o badge não recalculava o total exibido (site mostrava "Atacado aplicado" mas cobrava o preço cheio), confuso pro operador. Trocado por um **checkbox por item no carrinho do PDV**, habilitado só quando a quantidade bate o mínimo: o operador decide se aplica o desconto, e o preço/total do item mudam ao vivo. `items.*.apply_wholesale` (novo campo opcional em `StoreSaleRequest`) — o backend só usa `wholesale_price` se o operador marcou o checkbox **e** a quantidade ainda bate o mínimo; nunca automático.
- [x] **Troca de vendedor no PDV (F3)**, adiantada da Sprint 4.2 a pedido do usuário. `seller_id` já era um campo independente do usuário logado desde a Sprint 3 (só precisava de UX); F3 abre um modal de busca (mesmo padrão do F2 de produto: `BaseModal`, busca por nome, lista de `/users/active`) para trocar o vendedor sem precisar abrir o painel "Configuração do pedido". Cabeçalho do PDV passa a destacar o vendedor ativo (com a dica "F3" ao lado) e mantém o usuário logado como informação secundária ("Sessão") — sem re-autenticação, é só reatribuição do `seller_id` da venda.
- [x] Testes: 8 testes novos em `CancelSaleTest` (autorização, estorno de estoque/caixa, dupla-cancelamento, caixa fechado, motivo obrigatório, filtro por status), 3 em `RemoveCashOperationTest` (guarda contra remoção de lançamento de venda/estorno), cobertura de estoque insuficiente e de preço de atacado em `RegisterSaleTest`, `new_quantity` negativo em `AdjustStockTest` — 189 testes no total (13 novos), tudo verde.
- [x] *Extra pós-sprint:* seeder de 3 vendedores de demonstração (`ProductCatalogSeeder`, idempotente via `firstOrCreate`) — só pra ter opções reais no seletor do F3 sem precisar cadastrar manualmente a cada reset do banco.

## Sprint 4.2: Cotação/Orçamento (Fase 3.5) — ✅ CONCLUÍDA (2026-07-13, branch `feat/sprint-4.2-cotacao-orcamento`)
Item do PM que ficou de fora da Sprint 4.1 por ser o que precisa de mais desenho (já era um item candidato desde a Sprint 4 — ver `04-roadmap.md`, Fase 3.5).
- [x] Reaproveitar o carrinho do PDV, mas persistir como `SaleStatus::Pending` em vez de `Completed` — sem baixar estoque nem lançar no caixa. `RegisterQuoteAction` (novo) reaproveita a mesma lógica de precificação/desconto/atacado de `RegisterSaleAction` via um trait compartilhado (`Concerns\BuildsSaleItems`), mas pula a trava/exigência de caixa aberto e a checagem de estoque suficiente — orçamento é só uma cotação de preço, sem garantia de disponibilidade. `sales.cash_register_id`/`payment_method_id` passaram a nullable (consolidado direto na migration de criação da tabela, já que nada estava em produção — decisão do usuário) para acomodar isso.
- [x] Numeração própria para orçamento (`O000001`), distinta do número de venda (`V000001`) — decisão do usuário para não confundir um orçamento nunca convertido com uma venda real na conferência de caixa.
- [x] Tela própria de listagem de cotações (`pages/quotes.vue`, mesmo padrão visual de `sales-history.vue`), com prazo de validade (`sales.expires_at`, novo campo nullable) — filtros por vendedor/status/período, badge "Vencido" quando a validade passou, ações "Ver detalhes"/"Converter em venda"/"Cancelar" por linha. Listagem discrimina orçamento de venda normal via `GET /sales?is_quote=1` (`whereNull('cash_register_id')` — todo orçamento, convertido ou não, sempre teve esse campo nulo na própria criação).
- [x] Botão "Converter em venda" (`ConvertQuoteToSaleAction`, novo) que só então chama o `RegisterSaleAction` já existente sem alterações — reaproveitando, inclusive, o preço de atacado já implementado na Sprint 4.1. **Decisão do usuário:** a conversão cria uma **venda nova** (número `V`, estoque decrementado, caixa lançado, forma de pagamento escolhida na hora) vinculada ao orçamento original via `sales.converted_to_sale_id`/`converted_at` — o orçamento original nunca é apagado nem mutado em venda, só marcado `SaleStatus::Converted` (novo valor do enum), mesmo espírito "nunca apaga nada" do cancelamento (Sprint 4.1). Rejeita conversão de orçamento vencido ou já convertido/cancelado.
- [x] Cancelamento de orçamento pendente (extra, pedido do usuário durante o desenho desta sprint): `CancelSaleAction` ganhou um branch para `status === Pending` — cancela só com flip de status/motivo, sem reversão de estoque/caixa (nunca os tocou) e sem exigir caixa aberto, reaproveitando o mesmo endpoint/UI de cancelamento de venda (`POST /sales/{sale}/cancel`).
- [x] Substitui o item de menu "Pedidos" (antes "Em breve" na sidebar) por "Orçamentos", apontando para `/quotes`.
- [x] `pos.vue`: o gate que bloqueava a tela inteira sem caixa aberto foi afrouxado — carrinho/busca/desconto/cliente/vendedor funcionam sempre; só a forma de pagamento/valor recebido/"Finalizar Venda" ficam condicionados a `cashRegisterOpen` (com aviso inline em vez do bloqueio de tela cheia). Novo botão "Salvar Orçamento", habilitado com o carrinho não vazio independente do caixa, abre um modal só com o campo de validade opcional.
- [x] Testes: `QuoteTest` (criação sem caixa aberto, não toca estoque/caixa, ignora disponibilidade de estoque, número `O`, validade opcional/rejeita data passada, filtro `is_quote`), `ConvertQuoteTest` (caminho feliz, exige caixa aberto, rejeita estoque insuficiente na conversão, rejeita orçamento já convertido/vencido, autorização por papel, exige forma de pagamento), `CancelQuoteTest` (cancela sem caixa aberto e sem tocar estoque/caixa, rejeita cancelar orçamento já convertido, autorização por papel) — 208 testes no total no backend (23 novos), tudo verde.

## Sprint 5: Relatórios e Dashboard (Fase 4) — ✅ CONCLUÍDA (2026-07-14, branch `feat/sprint-5-relatorios-dashboard`)
- [x] Endpoints de relatório: vendas por dia, por produto, por vendedor, por categoria. *`ReportController` (novo, sem Actions — leitura pura agregada não se enquadra no padrão "operação de negócio" do doc `02-design-patterns.md`; reuso via query scopes `Sale::completed()`/`ProductVariation::lowStock()` nos Models). `GET /reports/sales-by-day|sales-by-product|sales-by-seller|sales-by-category`, liberado a qualquer papel autenticado (mesmo espírito de `stock-movements`).*
- [x] Alertas de estoque abaixo do mínimo (`current_quantity < min_quantity`, regra estrita). `GET /reports/low-stock` (`LowStockVariationResource`) e `low_stock_count` no `dashboard-summary`.
- [x] Dashboard inicial no Nuxt (`GET /reports/dashboard-summary`): vendas/faturamento do dia, vendas por vendedor hoje, alertas de estoque, gráficos "Vendas por mês"/"Valor em vendas por mês" (últimos 6 meses) — `index.vue` sai dos placeholders fixos. Cards "Giro de estoque"/"Ranking"/"Vendas 30 dias" de `products/index.vue` passam a consumir `sales-by-product` de verdade. Texto de boas-vindas e botão "Nova Venda" do dashboard, que ainda diziam "vendas, caixa e relatórios chegam nas próximas fases" (resquício da Sprint 0/1), atualizados/religados agora que os módulos existem.
- [x] *Extra (pedido do usuário durante a sprint, referência visual `Relatorios.dc.html` do projeto AppLoja no Claude Design):* tela dedicada `pages/reports.vue` — central de relatórios em 2 níveis (categorias Vendas/Estoque → cards de relatório → detalhe com filtro de período), religando o item "Relatórios" da sidebar (grupo Administração) e o atalho do dashboard, antes ambos placeholders "Em breve". Catálogo de 7 relatórios: Vendas Totais, por Produto, por Categoria, por Vendedor, Lucro Bruto por Produto (Vendas) + Nível de Estoque, Valor do Estoque (Estoque) — `GET /reports/catalog/{key}`. Escopo reduzido em relação ao mockup original por decisão do usuário: categoria "Financeiro" (Crediário, Contas a Receber, Comissões) ficou de fora por depender de `accounts_receivable`/`installments`, ainda não implementada (Sprint 6); "Frequência de Compra" e "Produtos Parados" (analytics mais complexos) também ficaram fora.
- [x] *Extra:* exportação **PDF** (`mpdf/mpdf` — biblioteca escolhida pelo usuário, mais leve/atual que `dompdf`; exigiu adicionar `ext-gd` à imagem `docker/php/Dockerfile`, requisito da v8.x) e **Excel** — `GET /reports/catalog/{key}/export/pdf|excel`, botões "Exportar PDF"/"Exportar Excel" em `reports.vue` (abrem em nova aba via `window.open`, mesma origem via nginx, cookies de sessão aplicados automaticamente — mesmo padrão do comprovante térmico da Sprint 3). Biblioteca de Excel trocada durante a sprint: `openspout/openspout` (escolha inicial, leve/streaming) → **`phpoffice/phpspreadsheet`**, a pedido do usuário, para replicar o "papel timbrado" da referência visual do AppLoja — nome/CNPJ/endereço/contato da loja (`store_settings`, mesma fonte do comprovante térmico) mesclados no topo, logo embutido via `PhpOffice\PhpSpreadsheet\Worksheet\Drawing` (só o PhpSpreadsheet insere imagem no `.xlsx`, o `openspout` não suporta), título do relatório centralizado, cabeçalho de tabela com fundo cinza e bordas. Decisão registrada: para o volume de dados de um relatório de loja única (uso esporádico, dezenas/poucas centenas de linhas), a vantagem de memória do `openspout` (streaming linha a linha) não se aplica — o footprint maior do `PhpSpreadsheet` (monta a planilha inteira em objetos antes de salvar) é irrelevante nessa escala. `ReportController@letterhead()` (novo, privado) monta os dados do cabeçalho e é reaproveitado tanto pelo PDF (`resources/views/reports/export.blade.php`, mesmo bloco de cabeçalho com logo via `<img>`) quanto pelo Excel (`App\Exports\ReportArrayExport`, reescrito).
- [x] *Extra (refinamento pós-comparação com um export real de referência trazido pelo usuário):* o Excel gerado inicialmente ficou "torto" (colunas espremidas num canto, sobrando espaço em branco à direita). `App\Exports\ReportArrayExport` passou a calcular a largura de cada coluna proporcionalmente ao conteúdo e depois **escalar tudo pra preencher uma folha A4 inteira** (94 unidades de largura em retrato, 141 em paisagem — a orientação muda sozinha pra paisagem a partir de 4 colunas de dados), em vez de larguras fixas por conteúdo isolado; cada relatório do catálogo assim "molda" a própria página conforme o nº de colunas que tem. Cabeçalho da tabela ajustado pra bater com o padrão comum de planilha de gestão (fundo cinza `#F0F0F0`, borda preta fina em cima **e** embaixo do cabeçalho, sem linha separadora entre as linhas de dado — mais limpo). O `fitToPage` da planilha foi desligado explicitamente (senão o Excel comprime tudo pra 1 página na hora de imprimir, escondendo a largura real). *Achado no processo:* o usuário trouxe um `.xlsx` real do AppLoja como referência (`docs/referencia-relatorio.xlsx`, removido do repo depois de analisado — 8,5MB, não deveria ser versionado); ao abrir o zip interno, o arquivo revelou ser gerado por uma ferramenta de relatório legada (padrão Delphi/FastReport) que desenha a "planilha" como se fosse uma página impressa — 22MB de XML, 227 mil strings únicas, 391 imagens vetoriais (EMF) sobrepostas, colunas de frações de pixel. Decisão registrada: **não replicar essa técnica** (contraria o princípio do projeto de evitar engenharia desnecessária, geraria arquivos enormes/lentos, e tornaria os dados não filtráveis/não somáveis no Excel — o oposto do que uma exportação de dados deveria fazer); em vez disso, aproveitou-se do `styles.xml` do arquivo (a única parte pequena e legível) só os detalhes de estilo aplicáveis a células nativas de verdade (cor de fundo do cabeçalho, borda dupla).
- [x] *Extra (segunda rodada de ajuste, após o usuário abrir o `.xlsx` numa planilha de verdade):* mesmo com largura proporcional, o bloco "papel timbrado" em células mescladas ficava visualmente solto — a grade do Excel continua além da última coluna de dados (só um modo de impressão/PDF tem borda de página; a visão normal da planilha não), então a linha divisória preta parecia "pendurada" no meio da tela. Trocado por uma **única imagem PNG gerada pelo próprio backend via GD** (`ReportArrayExport::buildLetterheadImage()`, usa `DejaVuSans`/`DejaVuSans-Bold` já empacotadas com o `mpdf/mpdf`, sem depender de upload nem de fonte nova) — logo + nome/CNPJ/endereço/contato + linha divisória desenhados dentro da própria imagem, inserida como um único `Drawing` na célula A1. Fica autocontida independente da largura da grade abaixo, sem células mescladas pra manter nem risco de texto cortado.
- [x] *Extra (terceira rodada — impressão real a partir do Excel não preenchia a página e vazava pra uma 2ª página em branco):* causa raiz era o `fitToPage` que tinha sido desligado de propósito na rodada anterior (rodando só contra o LibreOffice headless, que ignorava a flag por um detalhe de XML — ver achado registrado acima). `getPageSetup()` agora liga `setFitToPage(true)` com `setFitToWidth(1)`/`setFitToHeight(0)` (encaixa a largura em exatamente 1 página, altura livre pra crescer por quantas páginas o relatório precisar) e define a área de impressão explicitamente (`setPrintArea`, só o intervalo realmente usado) — sem isso o Excel imprimia nas larguras "naturais" das colunas, que raramente batem com a largura útil da folha. Aproveitado pra aumentar a imagem do papel timbrado (900×160px de origem, exibida a 115px de altura — antes 90px, ficava pequena) a pedido do usuário.
- [x] *Extra (quarta rodada — alinhamento "colado" entre colunas vizinhas):* o `PhpSpreadsheet` detecta sozinho o tipo da célula a partir do valor (`setCellValue`) — uma quantidade como `"10"` virava número de verdade (alinhado à direita, padrão do Excel), enquanto um valor monetário como `"R$ 199,00"` continuava texto (alinhado à esquerda), deixando colunas vizinhas com alinhamentos opostos "coladas" uma na outra sem respiro visual, ao contrário da referência (todas as colunas numéricas/monetárias com o mesmo alinhamento à direita). Corrigido em duas frentes: `setCellValueExplicit(..., DataType::TYPE_STRING)` em vez de `setCellValue` (elimina a detecção automática de tipo, todo valor vira texto de propósito) e um novo `columnAlignments()` que decide, célula a célula pelo conteúdo real da coluna (regex `/^-?(R\$\s?)?[\d.,]+%?$/`), se a coluna é numérica (direita) ou texto (esquerda) — aplicado tanto no cabeçalho quanto nos dados, pra ficarem alinhados um com o outro.
- [x] *Extra:* seeder de dados reais da loja (`ProductCatalogSeeder::seedStoreSettings()`, roda junto com `db:seed --class=ProductCatalogSeeder`) — nome/razão social/CNPJ/endereço/telefone/e-mail iguais aos da referência visual do AppLoja (JP Parafusos e Acessórios), incluindo o logo (`database/seeders/assets/jp-logo.png`, copiado do projeto de design pro disco `public` via `Storage::disk('public')->put()`, mesmo fluxo do upload real de logo da Sprint 0) — permite comparar lado a lado o cabeçalho dos relatórios exportados com dados de verdade em vez do "Minha Loja" genérico.
- [x] Testes: `backend/tests/Feature/Report/` (novo) — feature tests dos 6 endpoints básicos (agregações corretas, exclusão de vendas `pending`/`canceled`/só a venda nova de um `converted` conta, filtros de período/vendedor/categoria, autorização 401 para guest) e `ReportCatalogTest` (os 7 relatórios do catálogo via `#[DataProvider]`, JSON + export PDF + export Excel, chave inexistente retorna 404) — 38 testes novos, 247 no total no backend, tudo verde.

## Sprint 5.5: Pagamento dividido no PDV (split payment)
Item adiado conscientemente desde a Sprint 3 (`docs/05-sprints.md`, "Pagamento único por venda nesta entrega — split payment/parcelas e modal 'Ajustes' ficam para uma iteração futura"). Registrado como sprint própria em 2026-07-14 a pedido do PM: clientes que pagam parte em PIX e parte em dinheiro, ou parte em cartão e parte em dinheiro — hoje o PDV só aceita uma forma de pagamento por venda.
- [ ] Nova tabela `sale_payments` (`sale_id`, `payment_method_id`, `amount`) — `sales.payment_method_id` passa a nullable/legado (mantido só para vendas já existentes, não gravado em vendas novas) ou é removido, a decidir na hora (impacto em `SaleResource`/relatórios existentes).
- [ ] `RegisterSaleAction`/`RegisterQuoteAction` (via `Concerns\BuildsSaleItems` ou trait equivalente): aceitar `payments: [{ payment_method_id, amount }]` em vez de um único `payment_method_id`, validar que a soma bate exatamente com `sales.total` (dentro da mesma `DB::transaction()` já existente).
- [ ] PDV (`pos.vue`): trocar o seletor único de forma de pagamento por uma tela/modal de "múltiplos pagamentos" — adicionar linha (forma + valor), mostrar saldo restante a pagar, bloquear "Finalizar Venda" enquanto a soma não fechar com o total.
- [ ] Comprovante térmico (`resources/views/receipts/show.blade.php`): listar todas as formas de pagamento usadas na venda, não só uma.
- [ ] Cash register (`cash-register.vue`/`CashOperationResource`): `RegisterSaleAction` hoje lança uma única `cash_operations` por venda — decidir entre uma operação por forma de pagamento (mais fiel ao Kardex de caixa) ou manter uma operação só com o total e detalhar as formas no modal "Ver itens da venda" já existente.
- [ ] Relatórios (Sprint 5, `ReportController`): o relatório "Formas de Pagamento" fica reservado para esta sprint — hoje não existe porque `sales.payment_method_id` é 1:1; com `sale_payments`, a agregação passa a somar por linha de pagamento, não por venda.
- [ ] Testes: feature tests do fluxo de venda com pagamento dividido (soma exata, soma que não bate rejeitada, 1 forma continua funcionando como caso trivial), atualização dos testes existentes de `RegisterSaleTest`/`ReceiptTest` que hoje assumem `payment_method_id` único.

## Sprint 6 (opcional — Fase 5, após núcleo estável em produção)
- [ ] Crediário: `accounts_receivable` + `installments`, tela de lançamento e baixa de parcelas.
- [ ] `accounts_payable` + `expenses`, tela de lançamento.
- [ ] Entrada de estoque via XML de nota fiscal do fornecedor (`stock_entries`).
- [ ] Testes: feature tests para os fluxos acima, seguindo o mesmo padrão de cobertura das sprints anteriores.

## Melhorias transversais (encaixar quando conveniente, sem travar as fases)
- [ ] Upload do backup ao Google Drive (camada 2 — rclone ou disk adicional do laravel-backup).
- [ ] `deploy.sh` encapsulando o runbook de atualização da loja (ver `07-dev-environment.md`).
- [x] CRUD de usuários (login/papel/comissão/ativo) no back-office — tela "Usuários e Permissões" em Administração, admin-only, com trava de auto-desativação (`SelfDeactivationException`). *Vendedores* (perfil comercial mais completo — CPF/CNPJ, endereço, data de contratação, vínculo opcional com um usuário de login) é uma entidade **separada**, ainda não implementada — ver item abaixo.
- [ ] Tela "Vendedores" (Cadastros) — perfil de equipe comercial distinto de usuário de login: nova tabela `sellers` (nome, documento com toggle PF/PJ, contato, endereço, comissão, data de nascimento/contratação, observações, `user_id` nullable pra associar a uma conta de login existente).
  - [ ] Depois que `sellers` existir, voltar no modal de "Usuários e Permissões" (`frontend/app/pages/users.vue`) e adicionar a seção "Dados do vendedor" quando o papel selecionado for Vendedor — igual à referência do AppLoja: campo "Associar vendedor" (linka a um `seller` já existente) ou preenche os dados ali mesmo pra criar um `seller` novo já vinculado ao `user_id` recém-criado. Não dá pra fazer isso antes de `sellers` existir — depende da tarefa acima.
- [ ] Tela "Etiquetas" (Estoque e Compras) — impressão de etiqueta de preço/código de barras por produto/variação, reaproveitando `product_code`/`ean_gtin` já existentes em `product_variations` (sem tabela nova; parecida em espírito com a rota Blade do comprovante térmico da Sprint 3). Referência visual: `Etiquetas.dc.html` (projeto AppLoja no Claude Design). Placeholder "Em breve" adicionado ao sidebar (grupo "Estoque e Compras") na Sprint 4.
- [ ] Substituir `confirm()` nativo do navegador por um modal de confirmação padronizado do próprio design system (reaproveitar o padrão de modal com motivo já usado no cancelamento de venda/orçamento — `sales-history.vue`/`quotes.vue` — ou uma versão mais simples só com "Confirmar"/"Cancelar" quando não precisar de motivo). Pedido do usuário (2026-07-13): hoje 8 pontos usam `confirm()` do navegador (visual inconsistente com o resto do app, sem os tokens de cor/tipografia) — `components/settings/CatalogEntityCard.vue`, `pages/customers.vue`, `pages/suppliers.vue`, `pages/products/index.vue` (3 ocorrências: produto, variação/SKU em duas telas), `pages/cash-register.vue` (2 ocorrências: fechamento de caixa, remoção de lançamento). Não há `alert()` nativo no código hoje, só `confirm()`.
- [ ] Preferências de aparência (tema claro/escuro + tamanho de fonte) — pedido do usuário (2026-07-13), transversal (não pertence a nenhuma Fase de negócio do `04-roadmap.md`). Decisões já fechadas com o usuário:
  - **Persistência por usuário** (não por navegador/localStorage): `users` ganha `theme` (enum `light`/`dark`, default `light`) e `font_scale` (enum `small`/`medium`/`large`, default `medium`). Segue o login em qualquer terminal da loja.
  - Endpoint de autoatendimento — cada usuário edita a própria preferência (não é o `UserController` admin-only já existente): algo como `PUT /me/appearance`, `AppearancePolicy`/regra "sempre pode editar a si mesmo", sem precisar de papel especial.
  - **UI na topbar**, sempre visível (não em Configurações): toggle sol/lua ao lado do usuário + botões A-/A+ de fonte, acessível inclusive no PDV.
  - Dark mode exige definir os tokens escuros em `docs/08-design-system.md` (hoje só o tema claro está especificado — ver nota "Dark mode via toggle explícito só entra se for pedido no futuro", já superada) — derivar `--surface`/`--surface-raised`/`--border`/`--txt-*` escuros mantendo a separação marca vs. semântica (amarelo de marca provavelmente precisa de ajuste de saturação/luminosidade no escuro; cores de status via variantes `dark:` do Tailwind).
  - Tamanho de fonte: 3 passos (P/M/G) aplicados via `font-size` no `<html>` (ou variável CSS multiplicadora), não zoom do navegador — precisa validar que os layouts densos (tabelas, PDV) não quebram no passo "G".
  - Aplicar em `useAuthStore`/plugin de bootstrap (seta `data-theme` e o passo de fonte assim que `/api/me` carrega, antes da primeira pintura, pra evitar flash de tema errado).
